<%= form_for(order) do |f| %>
  <% if order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
      First Name: <%= f.collection_select( :customer_id, Customer.all, :id, :first_name) %>
  </div>
<br>
  <div class="field">
      Edition Name: <%= f.collection_select( :edition, Edition.all, :id, :name) %>
  </div>
<br>
  <div class="field">
    <%= f.label :amount %>
    <%= f.text_field :amount %>
  </div>

  <div class="field">
    <%= f.label :ad_size %>
    <%= f.text_field :ad_size %>
  </div>
<br>
  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
